package lab1

import groovy.transform.TypeChecked

/**
 * @author Evgeny Borisov
 */
class Start {
    public static void main(String[] args) {
        Character x='a'
        int i = x * x
        println(Math.sqrt(i))

//        def human = new Human(names: ['john', 'jacob', 'yacov'])
//        human.each { println(it) }
    }
}
